# GeoCheck - Telegram Mini App для отслеживания сотрудников

Полнофункциональное Telegram Mini App для отслеживания местоположения сотрудников с использованием геозон.

## 🚀 Возможности

- **Директор:**
  - Создание рабочих зон на интерактивной карте
  - Просмотр истории проверок сотрудников
  - Дашборд с фильтрацией успешных/неудачных проверок

- **Сотрудники:**
  - Получение случайных уведомлений о проверке местоположения
  - Отправка геолокации и фото через бота
  - Автоматическая проверка нахождения в рабочей зоне

- **Система:**
  - Автоматические случайные проверки (каждые 30 минут, 09:00-18:00)
  - Расчет расстояния до ближайшей зоны (формула Haversine)
  - Уведомления директора о нарушениях

## 📋 Технологии

- **Backend:** Node.js, Express, Telegraf, node-cron
- **Database:** PostgreSQL с Prisma ORM
- **Frontend:** React, Vite, TailwindCSS, react-leaflet
- **Deployment:** Railway (Docker)

## 🛠️ Установка и настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/marchelxyz/GeoCheck.git
cd GeoCheck
```

### 2. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен бота

### 3. Настройка базы данных

#### Локальная разработка:

```bash
# Установите PostgreSQL локально или используйте Docker
docker run --name geocheck-db -e POSTGRES_PASSWORD=password -e POSTGRES_DB=geocheck -p 5432:5432 -d postgres
```

#### Railway (Production):

1. Создайте новый проект на [Railway](https://railway.app)
2. Добавьте PostgreSQL сервис
3. Скопируйте `DATABASE_URL` из переменных окружения

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Заполните переменные:

```env
BOT_TOKEN=your_bot_token_from_botfather
DATABASE_URL=postgresql://user:password@host:port/database
ADMIN_PASSWORD=your_secure_password
WEB_APP_URL=https://your-app.railway.app
PORT=3000
```

### 5. Установка зависимостей

```bash
# Backend
cd server
npm install
cd ..

# Frontend
cd client
npm install
cd ..
```

### 6. Настройка базы данных

```bash
# Генерация Prisma Client
cd server
npm run prisma:generate

# Создание миграций
npm run prisma:migrate
```

### 7. Настройка Telegram Web App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Menu Button"
5. Установите URL вашего приложения (например: `https://your-app.railway.app`)

## 🏃 Запуск

### Локальная разработка

```bash
# Terminal 1 - Backend
cd server
npm run dev

# Terminal 2 - Frontend
cd client
npm run dev
```

Backend будет доступен на `http://localhost:3000`
Frontend будет доступен на `http://localhost:5173`

### Production (Railway)

1. Подключите репозиторий к Railway
2. Установите переменные окружения в Railway Dashboard
3. Railway автоматически соберет и развернет приложение

## 📱 Использование

### Для директора:

1. Откройте бота в Telegram и отправьте `/start`
2. Первый пользователь автоматически становится директором
3. Или используйте команду `/admin <password>` для получения прав директора
4. Нажмите кнопку "Открыть GeoCheck" для запуска Mini App
5. На вкладке "Карта зон" создайте рабочие зоны, кликая на карту
6. На вкладке "Проверки" просматривайте историю проверок

### Для сотрудников:

1. Откройте бота в Telegram и отправьте `/start`
2. Откройте Mini App - вы увидите экран ожидания
3. Когда придет уведомление о проверке, отправьте боту:
   - Ваше текущее местоположение (Live Location)
   - Фото (селфи или рабочее место)
4. Система автоматически проверит ваше местоположение

## 🔧 API Endpoints

### Публичные:
- `GET /health` - Проверка работоспособности

### Требуют авторизации (Telegram Web App):
- `POST /api/user` - Получить/создать пользователя
- `GET /api/user/role` - Получить роль пользователя
- `POST /api/admin/claim` - Получить права директора
- `GET /api/zones` - Список зон (только директор)
- `POST /api/zones` - Создать зону (только директор)
- `DELETE /api/zones/:id` - Удалить зону (только директор)
- `GET /api/check-ins` - История проверок (только директор)

## 📊 Структура базы данных

- **User** - Пользователи (директор/сотрудник)
- **Zone** - Рабочие зоны с координатами и радиусом
- **CheckInRequest** - Запросы на проверку
- **CheckInResult** - Результаты проверок с геолокацией и фото

## 🔐 Безопасность

- Проверка подлинности Telegram Web App данных
- Валидация всех входных данных
- Защита API endpoints от несанкционированного доступа
- Безопасное хранение паролей и токенов

## 📝 Cron Job

Система автоматически отправляет случайные проверки:
- **Расписание:** Каждые 30 минут
- **Время:** 09:00 - 18:00 (рабочие дни)
- **Логика:** Выбирает случайного сотрудника, который не проверялся последние 2 часа

## 🐛 Troubleshooting

### Бот не отвечает:
- Проверьте `BOT_TOKEN` в переменных окружения
- Убедитесь, что бот запущен и работает

### Ошибки базы данных:
- Проверьте `DATABASE_URL`
- Убедитесь, что миграции применены: `npm run prisma:migrate`

### Mini App не открывается:
- Проверьте `WEB_APP_URL` в переменных окружения
- Убедитесь, что URL настроен в BotFather

## 📄 Лицензия

MIT

## 👨‍💻 Автор

marchelxyz
